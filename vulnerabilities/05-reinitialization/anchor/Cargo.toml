[package]
name = "reinitialization-anchor"
version = "0.1.0"
description = "Demonstrates reinitialization vulnerability in Anchor"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "reinitialization_anchor"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build"]

[dependencies]
anchor-lang = "0.30.1"